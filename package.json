{
  "name": "onchain-guard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "pnpm -r test && pnpm run test:root",
    "typecheck": "pnpm -r typecheck && pnpm run typecheck:root",
    "typecheck:root": "tsc --noEmit -p tsconfig.json",
    "test:root": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.23.0",
    "typescript": "^5.6.0"
  }
}
