# deps / build
node_modules/
dist/
build/
*.tsbuildinfo

# foundry — `forge create` writes these at the repo root when compiling
# exploit-demo/MaliciousToken.sol for the local anvil experiment
out/
cache/

# env / secrets
.env
.env.*
!.env.example

# logs / os
*.log
.DS_Store
coverage/

# pglite writes the example agent database here
.eliza-db/
