{
  "name": "@onchain-guard/bench",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Attack corpus + canary-scored hijack benchmark harness. Not published.",
  "scripts": {
    "test": "npx tsx --test test/*.test.ts",
    "typecheck": "npx tsc --noEmit -p tsconfig.json",
    "bench": "npx tsx --env-file-if-exists=../../.env src/index.ts",
    "live": "npx tsx src/live.ts",
    "e2e": "npx tsx --env-file-if-exists=../../.env src/e2e.ts",
    "pipeline": "npx tsx --env-file-if-exists=../../.env src/pipeline.ts"
  },
  "dependencies": {
    "chainward": "workspace:*"
  }
}
